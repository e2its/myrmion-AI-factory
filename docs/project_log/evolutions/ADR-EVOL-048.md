---
id: ADR-EVOL-048
title: One planning stage — never zero, never two
date: 2026-09-25
status: accepted
---

# ADR-EVOL-048: One planning stage

## Context

Issue #57, axis F of the 2026-09 evolution (#50). A plan is a governance artefact: it pre-commits the work, and the harness's plan approval is a real user ratification. Two opposite failures were observed downstream and the framework had no notion of either: **zero stages** — work on a branch class with no planning phase of its own (a fix, a platform chore) reaching governed paths with no plan and no approval, so the first the user saw of a decision was the diff — and **two stages** — a command that already owns a planning phase entering plan mode again, producing an approval marker that appears to cover decisions the user never made. Decision authority delegated by the user on 2026-09-25; in this framework repo the ADR of an evolution is the branch's plan artefact, and this one was written before the first governed write of its branch.

## Decision

Agent-internal choices (pick + surviving risk):

- **A pre-write gate on governed paths, one reader.** `scripts/gates/planning.py` behind `gate.py plan --path <file>` and the PreToolUse hook `check-plan-approval.sh` (Edit|Write): a write to a governed path on a branch class that has no planning phase of its own requires an approved plan; without one it blocks with the blocking exit status (2, stderr) and a humanised reason naming the resolution (LAW-08). Configuration in `config/quality.json → planning`: `governed_paths` (globs), `docs_exempt` (documentation targets need no plan), `gate_inputs` (documents a gate reads — the constitution, the setup record, the rules, the config, the manifest — governed like code, the carve-out from the exemption), `exempt_classes`, `plan_artefact`, `adoption_window_minutes`. Precedence: a gate input is governed whatever its extension; then the documentation exemption; then the governed globs.
- **Branch classes enumerated, fail closed.** The classes exempt because a framework phase owns their plan are listed — in a project: `feature`, `increment`, `train`, `sub-increment`, `epic` (CODESIGN, BLUEPRINT and IMPLEMENT --plan own their plan artefacts and their own gates) — and **every unlisted class is gated**: `fix`, `docs`, `chore`, `breaking`, a protected branch, an unrecognised name. In the framework repo no class is exempt: an evolution branch has no framework phase; its plan artefact is `docs/project_log/evolutions/ADR-{ID}.md` in `status: accepted`, written first.
- **Approval is a marker written only on the user's approval** — the PostToolUse hook `record-plan-approval.sh` on the harness's `ExitPlanMode` tool (which completes only when the user approves) writes `.claude/state/plan-approved-<branch>.json`; the recorder refuses any input that is not that hook event. Or the branch's own plan artefact in an approved state — **new or changed on the branch**: an artefact already on the base branch unchanged is another change's plan (the review pass proved a `fix/EVOL-047-typo` branch would otherwise borrow a shipped ADR). The recorder refuses a payload that is not the `ExitPlanMode` PostToolUse event, one taken under a permission mode with no human (`bypassPermissions`, `dontAsk`, `auto`), one on a class a framework phase already plans (a second receipt), and one whose session transcript does not carry that tool use; the marker keeps the permission mode, the transcript path, the tool-use id and the cwd. A plan approved just before the branch is cut (on a base branch) is **adopted once**, by the first gated branch born to receive it, within `adoption_window_minutes` — the branch's own base first; the advisory never consumes it; an expired or future-dated one is said, never silently used; an approval older than `approval_ttl_hours` no longer counts. `.claude/state/` is ignored on both sides (the template `.gitignore` now says so). Risk: the marker is a file; an agent can write one from the shell against every rule above. The transcript check raises the bar from two strings to a tampered harness record, and the marker's own fields name what was forged — a visible fraud, not a silent one.
- **Documentation targets are exempt, except gate inputs.** `**/*.md` and `docs/**` (root-anchored — `src/docs/x.py` is code) need no plan; the constitution, `docs/setup.md`, the rules, `config/**`, the governance manifest, the increment plan (it decides whether a branch is a protected train) are inputs of gates and stay governed. In the framework repo **every governed path is a gate input** — the `.md` files under `.claude/` and `.context/templates/` are the product, exactly as § Generation Standards 3 says; only `docs/` and `README.md` are documentation, and the plan artefact itself (the ADR) is never a gate input. In a project the governed paths are seeded from the same answer as the deployment trigger (Q33) plus the governance paths. Rejected: exempting every `.md` — the constitution is a `.md`.
- **A command that owns a planning phase never enters plan mode.** CODESIGN, BLUEPRINT, IMPLEMENT `--plan`, DEVOPS `--configure` and SETUP `--init` state it in their command files; the IOP states the rule for every command; and it is mechanical — the PreToolUse hook `check-plan-mode.sh` (`gate.py plan --enter`) refuses plan mode on a branch class a framework phase already plans. One stage, never two.
- **An advisory before the block.** The prompt-submit hook asks `gate.py plan --status` and emits `<planning-warning>` when the session is on a gated class with no approval, so the user is never surprised by the refusal.

## Consequences

- New module `scripts/gates/planning.py` (lock-step pair, delivered); `gate.py plan --path | --status | --record`; two hooks (`check-plan-approval.sh`, `record-plan-approval.sh`) wired in both `settings.json`; `planning` block on both config sides; the prompt-submit advisory in both `governance-onprompt.sh`.
- `framework_version` 7.4.0 → **7.5.0** (MINOR — additive; branch `feature/EVOL-048-one-planning-stage`).
- No new LAW. Both `CLAUDE.md` sides: § Pre-Action Gate gains "One planning stage".
- In this repo, every evolution branch now writes its ADR before its first governed write — the discipline this session adopts from here on.

## Alternatives considered

A plan marker written by the agent on its own judgement — rejected: that is zero stages with a receipt. Gating every write (documentation included) — rejected: a README typo owes a branch and a PR, not a plan. Exempting `fix/*` because fixes are small — rejected: the observed zero-stage failure was exactly a fix reaching governed paths.

## Operational Rule

No LAW added. Meta and project `CLAUDE.md § Pre-Action Gate` gain the "One planning stage" paragraph. Both lock-step sides updated; universal sections and the LAW corpus untouched.

## Verification record

`scripts/test-gates.sh` (37 unit tests; new: governed precedence — a gate input under docs is governed, a documentation path is exempt, a rule and a skill are gate inputs even as `.md`, `docs/**` is root-anchored (code under a docs folder is code), an absolute path inside the repo is relativised and one outside is not governed, a missing `planning` block is a fault except for the one write that can add it, and the advisory never faults; **the real framework config** governs the product (skills, instructions, commands, the template tree, the config, the scripts, the workflows) and exempts only the documentation and the plan artefact, with no class exempt; a feature passes by phase and refuses both a plan-mode approval (a second receipt) and plan mode itself; every unlisted class (`fix`, `chore`, `docs`, `breaking`, an unrecognised name, `main`) is gated; the recorder refuses a payload that is not the harness's event, one under a no-human permission mode, one whose transcript is missing or carries no such tool use, and writes the marker with its provenance; a slug collision is no approval; an own marker older than the TTL or future-dated is said and refused; a `main` approval is adopted once by the first gated branch, refused by the second, never taken from another working branch's marker, the branch's own base wins over another base, the advisory never consumes it, an expired, naive or future-dated one is said and never adopted; an unreadable marker is no approval; the plan artefact approves only in an approved state, only for its own branch, and never once it is already on the base branch unchanged; CLI exit codes incl. `--enter`). `scripts/test-hooks.sh` (89: the pre-write hook blocks a governed write on `fix/gated` with the reader's resolution, passes a documentation write, blocks a gate input under docs, passes a well-formed payload without a path, blocks an unreadable payload, blocks without python3, blocks with the reader absent; the recorder refuses a forged event, a no-human permission mode and a transcript without the tool use through the envelope, writes the marker silently from the harness's payload, says its loss when the reader is absent, refuses a second receipt on a phase-planned class; the plan-mode entry hook blocks a feature branch and passes a fix branch and passes with the reader absent; both `settings.json` wire the three hooks and the matcher equals the recorder's constant; the prompt hook carries `<planning-warning>` on a gated unapproved branch and not on a feature branch). `scripts/materialize-synthetic.sh` (54 checks: on the scratch project a feature's governed write passes by class, a fix's is blocked, its documentation write passes, the gate's own wiring is governed, the delivered recorder writes the marker silently from the harness's payload and the approval unblocks the write, the marker is ignored by the materialised `.gitignore`, a docs branch is blocked on a gate input and may enter plan mode, a feature branch never enters plan mode; all three hooks land executable). `check-lockstep-pairs`, `validate-governance --base main`, ADR sync both directions, `gate.py one-definition`, `gate.py profile --run`, full T2 suite — green locally and in CI.

**Agentic review (LAW-13) and Phase 0 audit** on 8c27d93: no blocker. code-reviewer 7 🟡 (a naive timestamp crashed the gate and silenced the advisory; paths outside the repo governed by segment; the framework's behavioural contracts exempt as documentation; the project's own hook wiring and the increment plan ungoverned; the forgery refusal nominal; `.claude/state/` not ignored downstream; the recorder silent with the reader absent) + nits; silent-failure-hunter 2 HIGH (a shipped ADR approving any later branch carrying its id; the behavioural contracts as documentation) + 5 MEDIUM (`docs/**` exempting code at any depth; outside paths refused with a misleading reason; the hook passing without python3; own markers never expiring; naive timestamps) + LOW (adoption alphabetical; hooks resolving paths against the session cwd; slug collision; the upgrade path); pr-test-analyzer 12 gaps incl. "never two" being prose only, a bypass-mode approval as a valid receipt, the settings wiring unasserted, two base markers, the vacuous adoption assertion; Phase 0 audit 9 🟡 (the same exemption hole; the upgrade never wiring the pair; the inverse hazard of a wired gate without the key; governed paths not seeded from Q33; the IOP paragraph splitting the list; stale hook inventories; the trivial-config carve-out; DEVOPS `--configure` and SETUP owning plans; branch-naming drift). Every item fixed at the root in the same PR with red cases.
