---
status: DRAFT   # DRAFT | IN_PROGRESS | APPROVED | INVALIDATED | REJECTED
feature_id: "{{FEATURE_ID}}"
title: "Preventive Defect Sweep — {{FEATURE_NAME}}"
sweep_date: "YYYY-MM-DD"
trigger: "{why the sweep was triggered — first deploy / post-architectural change / cascade reopen / on request}"
methodology: "Parallel Explore sub-agents — one per non-overlapping scope derived from applicable DC catalog"
analyst: "{agent/user that ran the sweep}"
scopes_searched: []   # e.g. [backend, frontend, infra, cross-cutting]
total_findings: 0
by_severity:
  critical: 0
  high: 0
  medium: 0
  low: 0
all_resolved_in_commit: null   # true | false | null (not applicable)

# Iteration model tracking
based_on_iteration: 1
based_on_schemas_version: 1
pending_iteration: null
pending_schemas_version: null
invalidated_sections: []
invalidated_by_iteration: null
invalidated_reason: null
cascade_source: null
cascade_timestamp: null
cascade_scope: []

# Generated by Phase: IMPLEMENT --build completion (or manual trigger) | Feature: {{FEATURE_ID}}
---

# Preventive Defect Sweep Report — {{FEATURE_ID}}

> **Purpose.** Runtime defect scan derived from `.claude/rules/defect-prevention.md`. Catches the class of defects invisible to static gates (lint / typecheck / SAST / unit tests). Gates `DEVOPS --deploy --env dev` via the PREVENTIVE-SWEEP gate issue — see [Factory-devops-provision-deploy.instructions.md](.claude/instructions/Factory-devops-provision-deploy.instructions.md) § Pre-Deploy Status Gate.

## Scope

- **Feature:** {{FEATURE_ID}}
- **Catalog version:** {.claude/rules/defect-prevention.md version at sweep time}
- **DCs searched:** list of DC numbers filtered from the catalog by `applicable_to: IMPLEMENT` and matching the feature's stack
- **Scopes derived:** one row per non-overlapping scope (backend, frontend, infra, cross-cutting, …)

## Findings by DC

For each DC searched, one section:

```
## DC-{N} — {DC name}

Severity: {BLOCKER | HIGH | MEDIUM | LOW | CLEAN}
Occurrences: {count}

{If occurrences > 0, list each finding:}

- `file:line` — {short description}
  Expected: {what the DC prevention check says}
  Actual: {what was found}
  Fix suggestion: {remediation}
```

If a DC has zero occurrences, mark it explicitly `CLEAN` — never skip a DC from the applicable set.

## Consolidated Summary

| Severity | Count | Notes |
|---|---|---|
| CRITICAL | {n} | Must resolve before closing the PREVENTIVE-SWEEP gate issue |
| HIGH     | {n} | Must resolve before closing the gate issue |
| MEDIUM   | {n} | Resolve or justify before gate closure |
| LOW      | {n} | Track in backlog but do not block |

**Gate verdict:** {APPROVED — zero open BLOCKER/HIGH | REJECTED — {count} unresolved} 

## Fix Application Log

Applied in priority batches (P0 BLOCKER → P1 HIGH → P2 MEDIUM). One commit per priority group. BVL re-run after all fixes. One CI redeploy at the end.

| Priority | DC | file:line | Fix commit | BVL result |
|---|---|---|---|---|
| P0 | DC-{N} | ... | {sha} | ✅ |

## Cascade Invalidation

If this report is ever marked `status: INVALIDATED` by the iteration model cascade (see [Factory-iteration-model/SKILL.md](.claude/skills/factory-iteration-model/SKILL.md) § CASCADE_PENDING_ITERATION), the PREVENTIVE-SWEEP gate issue is reopened with the `stale-after-cascade` label and this document must be regenerated by a fresh sweep. Section-level delta updates are NOT supported — this is a frozen point-in-time artefact.

<!-- Generated by Phase: IMPLEMENT post-build (or manual) | Feature: {{FEATURE_ID}} -->
