# Template B: Master `test_plan.md` Template (Successful Output)

```markdown
---
id: {{FEATURE_ID}}
status: DRAFT
date: [DATE]
approver: PENDING
---

# Test Cases: {{FEATURE_ID}}
**Base:** `spec_{{FEATURE_ID}}.feature`

## 0. Resolutions Log (Q&A Log)
> 📝 **Strategy History:** Why we test what we test (Traceability).

- **Q1:** [Question, e.g., Should we test concurrency?]
  - **A1:** [Decision, e.g., No, it's a low-traffic MVP.]
  - **Rationale:** [Agreed technical reason.]

## 1. Acceptance Criteria Verification (UAT)
> **Objective:** Validate that the software strictly complies with the PO's Gherkin scenarios (Business Compliance).

| ID | Gherkin Ref | Business Scenario | Expected Result (Business) |
|:---|:---|:---|:---|
| AC-01 | Scenario 1 | Successful Login | User redirected to Dashboard |
| AC-02 | Scenario 2 | Validation Error | "Required field" message visible |

## 2. Detailed Technical Test Plan
> **Objective:** Break the software. Edge cases, security and robustness (Technical Robustness).

| ID | Type | Technical Scenario | Input Data | Expected Result (Tech) |
|:---|:---|:---|:---|:---|
| TC-01 | Edge | Database Timeout | DB: Down | 503 Service Unavailable (Controlled error handling) |
| TC-02 | Security | SQL Injection in Login | User: `' OR 1=1 --` | 400 Bad Request (Sanitized Input) |
| TC-03 | Negative | Malformed JSON payload | Body: `{ "user": ... ` | 400 Bad Request |

### 2.1 API Integration Test Cases (Required if feature has HTTP endpoints)
> **Objective:** Validate HTTP endpoints against `contracts/` contracts and business edge cases.
> **Source of truth:** `contracts/` (OpenAPI/GraphQL/TypeScript schemas generated by ARCH) + `design.md` endpoint tables.
> **Implementation template:** `.context/templates/develop/api_test_template.md`

| ID | Endpoint | Method | Scenario | Input Data | Expected Status | Contract Ref |
|:---|:---|:---|:---|:---|:---|:---|
| TC-API-01 | /api/v1/auth/login | POST | Successful login with valid credentials | `{ email, password }` | 200 + token | contracts/openapi/{{CONTRACT_SLUG}}/v1.yaml |
| TC-API-02 | /api/v1/auth/login | POST | Failed login with invalid credentials | `{ email, wrong_password }` | 401 Unauthorized | contracts/openapi/{{CONTRACT_SLUG}}/v1.yaml |
| TC-API-03 | /api/v1/users/:id | GET | Get existing user | Path: valid ID | 200 + user object | contracts/openapi/{{CONTRACT_SLUG}}/v1.yaml |
| TC-API-04 | /api/v1/users/:id | GET | Get non-existent user | Path: non-existent ID | 404 Not Found | contracts/openapi/{{CONTRACT_SLUG}}/v1.yaml |
| TC-API-05 | /api/v1/users | POST | Create user with incomplete payload | `{}` | 400/422 Validation Error | contracts/openapi/{{CONTRACT_SLUG}}/v1.yaml |
| TC-API-06 | /api/v1/users | POST | SQL injection in email field | `{ email: "' OR 1=1 --" }` | 400 Bad Request | test_plan Section 2 TC-02 |

> **Note for QA Agent:** Generate one row per endpoint × relevant scenario combination.
> Map happy paths from `spec.feature` scenarios + error/edge cases from Section 2 general.
> The `TC-API-XX` IDs are referenced by `/DEV --plan` and `/IMPLEMENT --build` to generate executable tests.

## 3. UX & Accessibility Testing (Required for UI Features)
> **Objective:** Validate compliance with UX Constitution (.context/rules/ux-constitution.md).
> **Reference:** WCAG 2.1 AA + responsive + design tokens.

| ID | Test Case | Tool | Expected Result |
|:---|:---|:---|:---|
| UX-01 | Responsive Design (Mobile/Tablet/Desktop) | Manual + Lighthouse | Layout adapts correctly at 360px, 768px, 1920px |
| UX-02 | Touch Targets (Mobile) | Static Analysis | All interactive elements ≥44x44px |
| A11Y-01 | WCAG 2.1 AA Compliance | Axe-core | 0 violations (critical/serious) |
| A11Y-02 | Color Contrast Ratios | Axe-core | Text contrast ≥4.5:1, Large text ≥3:1 |
| A11Y-03 | Keyboard Navigation | Manual | All interactions accessible via Tab/Enter/Space |
| A11Y-04 | Screen Reader Compatibility | Manual (NVDA/VoiceOver) | All content announced correctly with semantic landmarks |

## 4. Brand & Layout Compliance (Required for UI Features)
> **Objective:** Validate brand identity consistency and layout architecture.
> **Reference:** #file:docs/rules/ux-constitution.instructions.md Section I (Brand Identity & Layout Constitution)

| ID | Test Case | Tool | Expected Result |
|:---|:---|:---|:---|
| BRAND-01 | Color Palette Compliance | Static Analysis (`scripts/ux-validation.sh --brand-check`) | All colors use brand tokens (text-brand-*, bg-brand-*, var(--brand-*)) |
| BRAND-02 | Typography Compliance | Static Analysis | All fonts use typography tokens (font-brand-primary, font-brand-secondary) |
| BRAND-03 | Logo Asset Verification | Manual | Logo displays correctly from {{BRAND_LOGO_PATH}}, fallback to placeholder if not provided |
| BRAND-04 | Dark Mode Support (if enabled) | Manual + Visual Regression | Colors adapt correctly between light/dark themes |
| LAYOUT-01 | Layout Component Mapping | Static Analysis | Pages use correct Layout per section (Dashboard→DashboardLayout, Auth→AuthLayout) |
| LAYOUT-02 | App Shell Consistency | Manual | Persistent header/footer/sidebar consistent across sections |
| LAYOUT-03 | Section Navigation | Manual | Nav pattern matches ux-constitution.instructions.md Section I.3 definition |

**Brand Enforcement Validation:**
- Enforcement Level: `{{BRAND_ENFORCEMENT_LEVEL}}`
- If BLOCKER: Failures in BRAND-01/02 → Block release
- If WARNING: Report violations, allow release with documented exceptions
- If MIXED: BLOCKER on main/release branches, WARNING on feature branches
```
