# Template C: Quality Report

Materialised by `QA --verify {{FEATURE_ID}} [{{INC-N}}]` as a checkbox-driven verification artefact. Auto-approval requires every `- [ ]` item to become `- [x]` and a verdict of `APPROVED`.

**Path is mode-aware:**
- Slice mode (`/qa --verify {{FEATURE_ID}} {{INC-N}}`): `docs/spec/{{FEATURE_ID}}/qa/qa_report_{{INC-N}}_{{timestamp}}.md`. Required for `slicing_strategy: incremental` features.
- Aggregate mode (`/qa --verify {{FEATURE_ID}}`): `docs/spec/{{FEATURE_ID}}/qa/qa_report_final_{{timestamp}}.md`. Sole report for `slicing_strategy: monolithic` features. For incremental features it cross-references slice reports via `aggregates:`.

```markdown
---
id: {{FEATURE_ID}}
status: DRAFT   # DRAFT | IN_PROGRESS | APPROVED | REJECTED | INVALIDATED
verdict: PENDING   # PENDING | APPROVED | REJECTED
date: [DATE]
auditor: "QA Agent"
report_kind: "qa_report_final" | "qa_report_increment"
report_scope: feature | increment-{{INC-N}}  # slice or aggregate (report-level scope; distinct from feature_scope inherited from spec.feature)
increment_id: null | "{{INC-N}}"             # populated when report_scope == increment-*
aggregates: []                                # list of qa_report_{{INC-N}}_*.md paths consumed (aggregate mode + slicing_strategy=incremental only)

# Iteration model tracking
based_on_iteration: 1
based_on_schemas_version: 1

# Push-based cascade fields — set by upstream --refine, marks report INVALIDATED to force re-run
pending_iteration: null
pending_schemas_version: null
invalidated_sections: []
invalidated_by_iteration: null
invalidated_reason: null
cascade_source: null
cascade_timestamp: null
cascade_scope: []

# Outcome counts — populated during --verify execution
checklist_total: 0
checklist_passed: 0
checklist_failed: 0
blocker_count: 0
warning_count: 0
---

# Quality Report — {{FEATURE_ID}}

## Verification Checklist

> Generated by `QA --verify` (see `Factory-qa-verify.instructions.md` § Verification Checklist Generation). Every `- [ ]` must become `- [x]` before auto-approval.

### Preflight
- [ ] [QA-PRE-1] Dependency allowlist validation
- [ ] [QA-PRE-2] Migration safety validation
- [ ] [QA-PRE-3] IaC governance validation
- [ ] [QA-PRE-DATA] Synthetic data verification (if UI feature + staging)

### Governance
- [ ] [QA-GOV-1] Protected paths drift detection
- [ ] [QA-GOV-2] Integration audit (system_resources + hardcoded config)
- [ ] [QA-GOV-3] Static audit (code quality + test coverage + standards)

### Test cases (one per test_plan.md acceptance criterion)
- [ ] [QA-TC-{id}] {description}

### Regression
- [ ] [QA-REG-1] Unit test suite execution
- [ ] [QA-REG-2] Integration test suite execution
- [ ] [QA-REG-3] Contract test suite execution

### Static (independent re-execution)
- [ ] [QA-STATIC-1] Lint
- [ ] [QA-STATIC-2] Type check
- [ ] [QA-STATIC-3] SAST

### DAST (if configured)
- [ ] [QA-DAST-1] Baseline scan
- [ ] [QA-DAST-2] Full scan (if test_plan.dast_scan_type == "full")
- [ ] [QA-DAST-3] API scan (if API contracts exist)

### Defect Prevention Catalog
- [ ] [QA-DC-{N}] {DC name}: {DC check} — one row per DC entry with `applicable_to: QA` matching the feature/stack

## 1. Static Analysis
Results of the independent re-execution of lint/typecheck/SAST.

## 2. Test Execution
Results of the independent re-execution of unit/integration/contract test suites. Include stderr hashes or evidence paths.

## 3. DAST Findings
(Only when DAST is configured.) HIGH/BLOCKER findings block the verdict; LOW/INFO are recorded but do not block.

## 4. Defect Prevention Findings
Occurrences of cataloged DC patterns found during QA review. Each finding links to the DC entry in `.claude/rules/defect-prevention.md` and, if actionable, a follow-up task in the backlog.

## 5. Final Verdict

**STATUS:** {APPROVED | REJECTED}
**Verdict:** {APPROVED | REJECTED}

Notes:
- Compliance with acceptance criteria (UAT): certified / pending corrections.
- Open items: {list or "none"}
- Next command after APPROVED: MERGE via PR → `DEVOPS --deploy --env prod {{FEATURE_ID}}`.
- Next command after REJECTED: `IMPLEMENT --fix {{FEATURE_ID}} "{{reason}}"`.

## Cascade Invalidation

If this report is marked `status: INVALIDATED` by the iteration model cascade (see [Factory-iteration-model/SKILL.md](.claude/skills/Factory-iteration-model/SKILL.md) § CASCADE_PENDING_ITERATION → qa_report branch), re-run `QA --verify {{FEATURE_ID}}`. Section-level delta updates are NOT supported — the report is regenerated fresh against the current artefact state.
```
