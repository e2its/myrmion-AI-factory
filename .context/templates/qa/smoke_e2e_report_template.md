---
status: DRAFT   # DRAFT | IN_PROGRESS | APPROVED | INVALIDATED | REJECTED
feature_id: "{{FEATURE_ID}}"
title: "Smoke E2E Report — {{FEATURE_NAME}}"
scope: "{{SCOPE}}"  # inherited from spec.feature.scope; SINGLE template for ALL scopes (EVOL-041). Execution mode per scope: browser steps (full-stack/frontend-only), caller-harness + state + observability (backend-only/integration), hybrid (full-stack with backend surface). QA --verify Gate 4 blocks if the materialised scope does not match spec.feature.scope.
dev_environment: "{ENV name from devops_plan, typically 'dev'}"
deploy_build_sha: "{git sha of the dev-deployed build being smoked}"
execution_date: "YYYY-MM-DDThh:mm:ssZ"
executor: "{user or agent running the smoke blocks}"
source_paths: "docs/spec/{{FEATURE_ID}}/user_journey.md § Section 3: Paths"
source_test_catalogue: "docs/spec/{{FEATURE_ID}}/test_plan.md"
total_blocks: 0
by_verdict:
  passed: 0
  failed: 0
  skipped: 0
  blocked: 0
overall_verdict: null   # PASS | FAIL — only PASS closes the SMOKE-E2E gate
# Currency (EVOL-044): what this verdict certified. Written by the phase from
#   python3 scripts/gate.py certify --subject tree --paths {{RUNTIME_PATHS}}
# The push gate and CI recompute it (gate.py currency); a moved subject = STALE = re-take, never re-bless.
certifies:
  subject: tree
  paths: ["{{RUNTIME_PATHS}}"]   # the runtime roots the verdict covers, e.g. "src/**", "tests/**"
  hash: "{{CERTIFY_HASH}}"


# Iteration model tracking
based_on_iteration: 1
based_on_schemas_version: 1
pending_iteration: null
pending_schemas_version: null
invalidated_sections: []
invalidated_by_iteration: null
invalidated_reason: null
cascade_source: null
cascade_timestamp: null
cascade_scope: []

# Generated by Phase: QA (SMOKE-E2E gate preparation, post DEVOPS --deploy --env dev) | Feature: {{FEATURE_ID}}
---

# Smoke E2E Report — {{FEATURE_ID}}

> **Anchoring model (DEC-4 / EVOL-041).** Smoke anchors on the **test catalogue**: each block is one journey **Path** (`user_journey.md § Section 3`), expanded transitively Paso → `**BDD Scenario:**` → test-plan TC (`Gherkin Ref`). The journey contributes route COMPOSITION; `test_plan.md` contributes the verifiable cases; this report records their execution on the dev-deployed build. Gates `QA --verify` pass — see [Factory-qa-verify.instructions.md](.claude/instructions/Factory-qa-verify.instructions.md) § Prerequisites Gate.

## Source Paths

One SMOKE-{N} block per named Path in `user_journey.md § Section 3`. Skipping a path is allowed only when the feature explicitly excludes it in dev (e.g. "admin-only path — admin role not seeded in dev").

| Path | Persona | Pasos | Smoke block | Included? | Reason if skipped |
|---|---|---|---|---|---|
| {{PATH_NAME}} | {{PERSONA}} | 1 → 2 | SMOKE-1 | ✅ | — |
| {{RECOVERY_PATH_NAME}} | {{PERSONA}} | 1 → 3 | SMOKE-2 | ✅ | — |

## Smoke Blocks

For each included path, one block. `Covered TCs` is the transitive expansion (Paso → BDD Scenario → every test_plan TC whose `Gherkin Ref` names that scenario) — it makes the block's verification scope mechanical, not improvised.

```
## SMOKE-{N} — Path "{path name}" ({persona})

Pasos: {ordered Paso list}
Covered TCs: {AC-XX / TC-XX ids joined transitively via Gherkin Ref}

Preconditions:
  - {seed data / env state required}
  - {auth token / session / caller credentials if any}

Steps (execution mode per scope):
  # full-stack / frontend-only → browser actions following each Paso's **Does**, asserting each Paso's **Sees**
  # backend-only / integration → caller-harness request per Paso + downstream state + observability triple-check
  1. [Paso 1] {action} → assert: {the Paso's Sees, in observable terms}
  2. [Paso 2] {action} → assert: {...}

Expected outcome:
  - {end-of-path business outcome — matches the Path's final Paso Sees + § 5 guarantee}

Actual outcome:
  - {what was observed}

Verdict: PASS | FAIL | BLOCKED
Evidence: {screenshot / response capture / log snippet — or path under docs/spec/{{FEATURE_ID}}/smoke_evidence/}
Notes: {anomalies, follow-ups — optional}
```

### Reliability blocks (scope in [backend-only, integration] — MANDATORY)

Additionally execute one `SMOKE-REL-{X}` block per REL-*-XX family in `test_plan.md § 2.2` (idempotency replay, retry exhaustion, circuit breaker, DLQ, graceful shutdown), whose mechanisms are defined in `design.md § 6 Reliability Contract`. Same block shape; `Covered TCs` lists the REL-*-XX ids.

## Consolidated Result

| Block | Path | Covered TCs | Verdict | Evidence |
|---|---|---|---|---|
| SMOKE-1 | {{PATH_NAME}} | AC-01, TC-API-01 | PASS | `docs/spec/{{FEATURE_ID}}/smoke_evidence/smoke-1.png` |
| SMOKE-2 | {{RECOVERY_PATH_NAME}} | AC-02, TC-API-02 | FAIL | `docs/spec/{{FEATURE_ID}}/smoke_evidence/smoke-2-response.json` — 500 on submit |

**Overall verdict:** `{PASS | FAIL}`

- `PASS` — every included block is PASS. Move the SMOKE-E2E gate issue to Done. `QA --verify` can now run.
- `FAIL` — at least one block is FAIL. Gate stays open. Fix the failing path, redeploy to dev (which cascades a new `pending_iteration` on this report — see below), and re-run the failing blocks.

## Cascade Invalidation

If `DEVOPS --deploy --env dev` redeploys the feature to dev after this report closes, the iteration model marks this report `status: INVALIDATED` and reopens the SMOKE-E2E gate issue with the `stale-after-cascade` label (see [Factory-iteration-model/SKILL.md](.claude/skills/factory-iteration-model/SKILL.md) § CASCADE_TRIGGERS → ON_DEVOPS_REDEPLOY_DEV). The document must be regenerated by re-running the smoke blocks against the new build. Section-level delta updates are NOT supported — this is a frozen point-in-time artefact tied to a specific `deploy_build_sha`.

<!-- Generated by Phase: QA (SMOKE-E2E gate) | Feature: {{FEATURE_ID}} -->
