---
status: DRAFT   # DRAFT | IN_PROGRESS | APPROVED | INVALIDATED | REJECTED
feature_id: "{{FEATURE_ID}}"
title: "Smoke E2E Report — {{FEATURE_NAME}}"
dev_environment: "{ENV name from devops_plan, typically 'dev'}"
deploy_build_sha: "{git sha of the dev-deployed build being smoked}"
execution_date: "YYYY-MM-DDThh:mm:ssZ"
executor: "{user or agent running the smoke blocks}"
source_scenarios: "docs/spec/{{FEATURE_ID}}/user_journey.md"
total_blocks: 0
by_verdict:
  passed: 0
  failed: 0
  skipped: 0
  blocked: 0
overall_verdict: null   # PASS | FAIL — only PASS closes the SMOKE-E2E gate

# Iteration model tracking (EVOL-014)
based_on_iteration: 1
based_on_schemas_version: 1
pending_iteration: null
pending_schemas_version: null
invalidated_sections: []
invalidated_by_iteration: null
invalidated_reason: null
cascade_source: null
cascade_timestamp: null
cascade_scope: []

# Generated by Phase: DEVOPS --deploy --env dev post-completion | Feature: {{FEATURE_ID}}
---

# Smoke E2E Report — {{FEATURE_ID}}

> **Purpose.** Numbered manual smoke blocks derived from `user_journey.md` BDD scenarios, executed against the dev-deployed build. Replaces ad-hoc smoke with a reproducible DoD artefact. Gates `QA --verify` pass — see [Factory-qa-verify.instructions.md](.claude/instructions/Factory-qa-verify.instructions.md) § Prerequisites Gate.

## Source Scenarios

List every numbered scenario from `docs/spec/{{FEATURE_ID}}/user_journey.md` that maps to a smoke block. Each scenario becomes exactly one SMOKE-{N} block below. Skipping a scenario is allowed only when the feature explicitly excludes it (e.g. "admin-only flow — skipped in smoke because admin role is not seeded in dev").

| Scenario ref | Smoke block | Included? | Reason if skipped |
|---|---|---|---|
| US-1.1 | SMOKE-1 | ✅ | — |
| US-1.2 | SMOKE-2 | ✅ | — |
| US-2.1 | SMOKE-3 | ⏭️ skipped | admin seeding not available in dev |

## Smoke Blocks

For each included scenario, one block:

```
## SMOKE-{N} — {scenario title from user_journey.md}

Preconditions:
  - {seed data / env state required}
  - {auth token / session state if any}

Steps:
  1. {action on UI or API}
  2. {action}
  3. {assertion}

Expected outcome:
  - {what must be visible / returned / logged}

Actual outcome:
  - {what was observed}

Verdict: PASS | FAIL | BLOCKED
Evidence: {link to screenshot, response capture, log snippet — or path under docs/spec/{{FEATURE_ID}}/smoke_evidence/}
Notes: {anomalies, follow-ups, observations — optional}
```

## Consolidated Result

| Block | Scenario | Verdict | Evidence |
|---|---|---|---|
| SMOKE-1 | US-1.1 | PASS | `docs/spec/{{FEATURE_ID}}/smoke_evidence/smoke-1.png` |
| SMOKE-2 | US-1.2 | FAIL | `docs/spec/{{FEATURE_ID}}/smoke_evidence/smoke-2-response.json` — 500 error on submit |

**Overall verdict:** `{PASS | FAIL}`

- `PASS` — every included block is PASS. Move the SMOKE-E2E gate issue to Done. `QA --verify` can now run.
- `FAIL` — at least one block is FAIL. Gate stays open. Fix the failing path, redeploy to dev (which cascades a new `pending_iteration` on this report — see below), and re-run the failing blocks.

## Cascade Invalidation

If `DEVOPS --deploy --env dev` redeploys the feature to dev after this report closes, the iteration model marks this report `status: INVALIDATED` and reopens the SMOKE-E2E gate issue with the `stale-after-cascade` label (see [Factory-iteration-model/SKILL.md](.claude/skills/Factory-iteration-model/SKILL.md) § CASCADE_TRIGGERS → ON_DEVOPS_REDEPLOY_DEV). The document must be regenerated by re-running the smoke blocks against the new build. Section-level delta updates are NOT supported — this is a frozen point-in-time artefact tied to a specific `deploy_build_sha`.

<!-- Generated by Phase: DEVOPS post-deploy-dev (or manual) | Feature: {{FEATURE_ID}} -->
